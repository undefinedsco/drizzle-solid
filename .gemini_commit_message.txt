feat(sparql): Implement and verify SPARQL endpoint support with Graph Scoping

- Core Logic:
  - Implement automatic Graph Scoping in `SparqlStrategy`: DML operations are now wrapped in `GRAPH <base>` or `WITH <base>`, ensuring data persistence in Named Graphs (Resource URIs).
  - Remove `LIMIT 1` from SPARQL ASK probe in `PodDialect` to fix CSS 500 compatibility.
  - Relax URI validation in `UriHandler` to support URN/UUID schemes.

- Examples & Docs:
  - Refactor `examples/` into `01-quick-start`, `02-relational-query`, `03-zero-config-discovery`.
  - Update `README.md` and `examples/README.md`.

- Integration Tests:
  - Add `tests/integration/css/examples-verification.test.ts` to ensure all examples run correctly in CI.
  - Add `tests/integration/css/sparql-endpoint.test.ts`: Verifies that SPARQL SELECT/INSERT/UPDATE work correctly with CSS `/-/sparql` endpoint (with known DELETE limitation logged).